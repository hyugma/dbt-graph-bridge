{{ config(
    materialized='node',
    labels=['Department'],
    unique_key='department_name',
    indexes=[
        {'properties': ['department_id']}
    ]
) }}

SELECT
    department_id,
    department_name,
    location
FROM {{ ref('stg_departments') }}
