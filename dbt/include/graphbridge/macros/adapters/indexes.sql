{% macro graphbridge__create_index(label, index_dict) %}
  -- This is implemented in Python (adapter.create_indexes), but this macro is kept for potential future Jinja usage
{% endmacro %}

{% macro graphbridge__create_constraint(label, constraint_dict) %}
  -- This is implemented in Python (adapter.create_constraints), but this macro is kept for potential future Jinja usage
{% endmacro %}
